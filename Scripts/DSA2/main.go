package main

import (
	"fmt"
)

func main() {
	//timeStampStr := "2023-09-20T17:14:12+05:30"
	//var calculateScore = false
	//timestamp, err := time.Parse(time.RFC3339, timeStampStr)
	//if err != nil {
	//	fmt.Println(err)
	//}
	//resetTime := time.Now().Add(-time.Minute * time.Duration(2440))
	//fmt.Println(timestamp, resetTime)
	//if resetTime.After(timestamp) {
	//	calculateScore = true
	//}

	fmt.Printf("User id = %d, Transaction = %d\n", 1, 1)

	return
}
